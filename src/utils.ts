
export const _ = require('lodash');
export const Promise = require('bluebird');
// var crypto = require('crypto');
// var atonic = require('atonic');
// var objHash = require('object-hash');

// function sha1(value) {
// 	return crypto.createHash('sha1').update(value, 'binary').digest('hex').toLowerCase();
// }

// function md5(value) {
// 	return crypto.createHash('md5').update(value, 'binary').digest('hex').toLowerCase();
// }

// function buildServiceOptions(options, defaults) {
// 	defaults = defaults || {
// 		format: 'json'
// 	};

// 	return _.defaults(options || {}, defaults);
// }

